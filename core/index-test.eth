(package eth/core-test ()

(import "../testing" (new-run run test before after before-each after-each))
(import "./index" (..))

(new-run)

(test "core: type" (fn ()
  (assert (== (type 1) :number) "type of 1 is :number")))

(test "core: type" (fn ()
  (assert (== (type 1) :number) "type of 1 is :number")))

(test "core: type" (fn ()
  (assert (== (type 1) :number) "type of 1 is :number")))

(test "throw" (fn ()
  (throw (Error. "Woow this was not expected !"))))

(test "core: type" (fn ()
  (assert (== (type 1) :number) "type of 1 is :number")))


(run)
)
